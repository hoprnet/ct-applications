apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: netwatcher
  namespace: staging
spec:
  project: staging
  source:
    repoURL: https://dysnix.github.io/charts
    targetRevision: 0.99.16
    chart: app
    plugin:
      env:
        - name: HELM_VALUES
          value: |
            image:
              repository: europe-west6-docker.pkg.dev/ctdapp-391309/ctdapp/cover-traffic
              tag: "0195a42"
            replicaCount: 1
            args:
              - python
              - '-m'
              - netwatcher
            env:
              AGG_POST: http://staging-aggregator.staging:8080/aggregator/list
              AGG_BALANCE: http://staging-aggregator.staging:8080/aggregator/balances
              API_HOST: <random_host>
              API_KEY: <random_key>
              LAT_COUNT: 10
              MOCK_MODE: True
            serviceAccount:
              create: false
              name: "default"
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
