apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: economic-handler
  namespace: staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: staging-economic-handler
  endpoints:
    - port: "http"
      interval: 30s
      path: /metrics